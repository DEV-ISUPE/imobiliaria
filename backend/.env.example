# Configuração para XAMPP (MySQL padrão: root sem senha, porta 3306)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=maison_huambo
DB_PORT=3306

# Socket (apenas para macOS/Linux com MySQL nativo; remover para XAMPP no Windows)
# DB_SOCKET=/tmp/mysql.sock

JWT_SECRET=maison-huambo-secret-key-2024
PORT=3000
